import React from "react";

const Loading = () => {
    return (
        <div className="flex justify-center">
            <button className="btn loading bg-transparent btn-square text-black border-none">
                Loading...
            </button>
        </div>
    );
};

export default Loading;
